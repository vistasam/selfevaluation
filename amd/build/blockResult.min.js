/**
 * // Fetch the captured results and present them in a graphical form
 * @module     block_selfevaluation/result
 * @copyright  2023 Rohit <rx18007@edu.rta.lv>
 * @license    http://www.gnu.org/copyleft/gpl.html GNU GPL v3 or later
 */
define("block_selfevaluation/blockResult",["jquery","core/ajax"],(function($,Ajax){return{init:function(statistics){$("#result-view").on("change",(()=>{let selectedId=parseInt($("#result-view").val()),selectedData=Object.values(statistics).find((item=>item.id===selectedId));selectedData&&selectedData.hasOwnProperty("title")&&selectedData.hasOwnProperty("values")?($(".values-content").empty(),selectedData.values.forEach(((value,index)=>{let paragraphId="values-".concat(index);$("#".concat(paragraphId)).text(function(value){let parts=value.split(","),min=parts[0].split("(")[0],max=parts[1].split("(")[0],avg=parts[2].split("(")[1].split(")")[0];return"Min - ".concat(min,", Max - ").concat(max,", Avg - ").concat(avg)}(value))}))):console.error("Invalid data for selected ID:",selectedId)}))}}}));

//# sourceMappingURL=blockResult.min.js.map